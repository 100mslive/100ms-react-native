export NODE_BINARY=/Users/yogesh/.local/share/nvm/v22.20.0/bin/node

# Enable Fabric (New Architecture) for TurboModules and Fabric components
# This matches the Android configuration (newArchEnabled=true)
export RCT_NEW_ARCH_ENABLED=1

# Metro bundler configuration for physical device testing
# Use Mac's local network IP so physical devices can connect to Metro
export RCT_METRO_PORT=8081
export METRO_HOST=192.168.1.3
